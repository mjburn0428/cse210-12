 # keys = pygame.key.get_pressed()
        # if keys[pygame.K_LEFT] and ship.x - player_vel > 0: # left
        #     ship.x -= player_vel
        # if keys[pygame.K_RIGHT] and ship.x + player_vel + ship.get_width() < SCREEN_WIDTH: # right
        #     ship.x += player_vel
        # if keys[pygame.K_UP] and ship.y - player_vel > 0: # up
        #     ship.y -= player_vel
        # if keys[pygame.K_DOWN] and ship.y + player_vel + ship.get_height() + 15 < SCREEN_HEIGHT: # down
        #     ship.y += player_vel
        # if keys[pygame.K_SPACE]:
        #     ship.shoot()